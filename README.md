# Golden Duck Authors Electric Archive

Automated, append-preserving archive for Julia Jones's Authors Electric posts.

The scheduled GitHub Action checks Authors Electric daily, keeps the previous last-known-good archive when a source fails, creates preserved article copies, mirrors post images, writes versioned metadata snapshots and deploys the public archive through GitHub Pages.

See `SETUP-INSTRUCTIONS.md` for the one-time browser-only setup.
